# URL base de signa-api. NO lleva /api: application.yaml no define context-path,
# las rutas del back son directas (/auth/..., /users/...).
# Android emulator: usar 10.0.2.2 en vez de localhost.
# iOS simulator: localhost funciona directo.
# Dispositivo fisico: usar la IP de tu PC en la red local (ej 192.168.0.10).
EXPO_PUBLIC_API_URL=http://10.0.2.2:8080